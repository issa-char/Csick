#include <stdio.h>

int main(void)
{
		char c;
		int d;
		float e;

		printf("the memory addr of variable c in memory is %p\n", &c);
		printf("the memory addr of variable d in memory is %p\n", &d);
		printf("the memory addr of variable e in memory is %p\n", &e);

		return(0);
}

